<!-- products.handlebars -->

<ul>
  {{#each products}}
    <div>
      <h2>{{this.title}}</h2>
      <p>Precio: {{this.price}}</p>
      <p>Categoría: {{this.category}}</p>
      <a href="/products/{{this.id}}">Ver detalles</a>
    </div>
  {{/each}}
</ul>